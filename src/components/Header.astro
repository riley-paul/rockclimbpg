---
interface Link {
  name: string;
  link: string;
  selected: (pathname: string) => boolean;
}

const links: Link[] = [
  { name: "Home", link: "/", selected: (pathname) => pathname === "/" },
  { name: "Home", link: "/", selected: (pathname) => pathname === "/" },
  { name: "Home", link: "/", selected: (pathname) => pathname === "/" },
];
---

<header class="bg-card border-b shadow px-6 py-2">
  RockClimbPG


</header>
